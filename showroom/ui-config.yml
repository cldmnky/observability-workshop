---
type: showroom

# Set the left column width to 30%
default_width: 30
# Persist the URL state so browser refresh doesn't reset the UI
persist_url_state: true

# OCP Console and Web Terminal cannot be embedded in an iframe (X-Frame-Options: DENY).
# Users open the console via links in the workshop content or a separate browser window.
